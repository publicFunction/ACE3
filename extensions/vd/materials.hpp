#pragma once

enum MATERIAL_TYPES {
    TUNGSTEN = 0,
    DEPLETED_URANIUM = 1,
    STEEL = 2
};

enum MATERIAL_HARDNESS {
    TUNGSTEN = 600,
    DEPLETED_URANIUM = 600,
    STEEL = 300
};

// kg/m3
enum MATERIAL_DENSITY {
    TUNGSTEN = 4800,
    DEPLETED_URANIUM = 4800,
    STEEL = 4800
};